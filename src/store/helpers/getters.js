export function apiHost(state) {
  return state.apiHost;
}
export function currentScreen(state) {
  return state.currentScreen;
}

export function uermLogo(state) {
  return state.uermLogo;
}

export function currentScreenProperty(state) {
  return state.currentScreenProperty;
}
export function currentLocation(state) {
  return state.currentLocation;
}

export function logoffUser(state) {
  return state.logoffUser;
}

export function breadCrumb(state) {
  return state.breadCrumb;
}

export function notification(state) {
  return state.notification;
}

export function darkMode(state) {
  return state.darkMode;
}

export function navLinks(state) {
  return state.navLinks;
}

export function scrollAreaThumbStyle(state) {
  return state.scrollAreaThumbStyle;
}

export function scrollAreaBarStyle(state) {
  return state.scrollAreaBarStyle;
}
